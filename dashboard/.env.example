# Shared with GitHub Actions (ingest auth) — REQUIRED in production
GOVERNANCE_DASHBOARD_SECRET=change-me-to-a-long-random-string

# Browser site gate (recommended on public Vercel Hobby / production URLs).
# When set, visitors must sign in at /login. Session cookie lasts 7 days.
# Separate from GOVERNANCE_DASHBOARD_SECRET (CI ingest only).
# GOVERNANCE_SITE_PASSWORD=change-me-site-password

# Required for human Approve/Reject/Merge actions. Must be different from
# GOVERNANCE_DASHBOARD_SECRET.
GOVERNANCE_REVIEWER_SECRET=change-me-to-another-long-random-string

# Local-only escape hatch (ignored when NODE_ENV=production)
# GOVERNANCE_ALLOW_INSECURE_DEV=true

# Required on Vercel (auto-injected when you add Upstash Redis from Marketplace)
# UPSTASH_REDIS_REST_URL=
# UPSTASH_REDIS_REST_TOKEN=
# (Vercel KV aliases also work: KV_REST_API_URL / KV_REST_API_TOKEN)

# Optional: GitHub token for Approve & Merge from the dashboard
# Fine-grained PAT: Contents + Pull requests
# GITHUB_TOKEN=
# or:
# GH_MERGE_TOKEN=

# Optional: public dashboard URL used in review deep links
# GOVERNANCE_DASHBOARD_PUBLIC_URL=https://your-dashboard.vercel.app

# Optional: restrict merge targets to this owner/name
# GITHUB_REPOSITORY=Alteoziel/Budget-App
